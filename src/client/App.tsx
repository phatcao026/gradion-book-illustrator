export function App() {
  return (
    <main className="page-shell">
      <section className="hero" aria-labelledby="page-title">
        <p className="eyebrow">Gradion assessment</p>
        <h1 id="page-title">Book Illustration Studio</h1>
        <p className="lede">
          The development harness is ready. The illustration workflow will be
          built in the next milestones.
        </p>
        <div className="status" role="status">
          <span className="status-dot" aria-hidden="true" />
          Frontend ready
        </div>
      </section>
    </main>
  );
}
